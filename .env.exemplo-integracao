# 🔗 Exemplo de .env para Integração Externa
# Copie este arquivo para .env e ajuste as configurações

# ===== CONFIGURAÇÃO DO SERVIDOR =====
PORT=3000
NODE_ENV=production

# ===== BANCO DE DADOS =====
# Para produção, use MongoDB Atlas ou servidor dedicado
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/cvgen-api?retryWrites=true&w=majority

# Para desenvolvimento local, use memory database
# MONGODB_URI=memory://localhost

# ===== SEGURANÇA JWT =====
# IMPORTANTE: Gere chaves únicas para produção!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua-chave-jwt-super-secreta-aqui-256-bits-minimo
JWT_REFRESH_SECRET=sua-chave-refresh-jwt-super-secreta-aqui-256-bits
JWT_EXPIRE=2h
JWT_REFRESH_EXPIRE=30d

# ===== CONFIGURAÇÃO DA API =====
# URL pública da sua API (onde os clientes vão acessar)
API_BASE_URL=https://cvgen-api.suaempresa.com

# Caminho para salvar os PDFs
PDF_STORAGE_PATH=./storage/pdfs

# ===== CORS - DOMÍNIOS AUTORIZADOS =====
# Adicione os domínios que podem acessar sua API
CORS_ORIGINS=https://mozdjob.com,https://app.mozdjob.com,https://cliente1.com,https://cliente2.com

# ===== RATE LIMITING =====
# Controle de requisições por minuto
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=500

# ===== CREDENCIAIS DO ADMIN =====
ADMIN_EMAIL=admin@suaempresa.com
ADMIN_PASSWORD=senha-super-segura-admin-2024

# ===== CONFIGURAÇÕES DE API KEY =====
# Tempo de expiração das API Keys (never = nunca expira)
API_KEY_EXPIRY=never
MAX_API_KEYS_PER_USER=10

# ===== WEBHOOKS (OPCIONAL) =====
# Para notificar aplicações externas quando CV for gerado
WEBHOOK_SECRET=webhook-secret-2024
WEBHOOK_TIMEOUT=30000

# ===== STORAGE EM NUVEM (OPCIONAL) =====
# Para usar AWS S3 em vez de storage local
# STORAGE_TYPE=s3
# AWS_ACCESS_KEY_ID=sua-access-key
# AWS_SECRET_ACCESS_KEY=sua-secret-key
# AWS_BUCKET_NAME=cvgen-pdfs
# AWS_REGION=us-east-1

# ===== MONITORAMENTO (OPCIONAL) =====
# Para tracking de erros e performance
# SENTRY_DSN=https://sua-sentry-dsn@sentry.io/projeto
# ANALYTICS_ENABLED=true

# ===== EMAIL (OPCIONAL) =====
# Para enviar notificações por email
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@suaempresa.com
# SMTP_PASS=senha-do-email

# ===== BRANDING (OPCIONAL) =====
# Personalização da marca
COMPANY_NAME=Sua Empresa
COMPANY_LOGO=https://suaempresa.com/logo.png
BRAND_COLOR=#2563eb

# ===== DEBUG =====
DEBUG=false
LOG_LEVEL=info